apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-experiment
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: chaosmesh-microservice
  duration: "30s"
  gracePeriod: 0
