apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-failure-experiment
  namespace: default
spec:
  action: pod-failure
  mode: fixed-percent
  value: "40"
  selector:
    labelSelectors:
      app: chaosmesh-microservice
  duration: "60s"
