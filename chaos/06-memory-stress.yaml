apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: memory-stress-experiment
  namespace: default
spec:
  mode: one
  selector:
    labelSelectors:
      app: chaosmesh-microservice
  stressors:
    memory:
      workers: 2
      size: "128MB"
  duration: "60s"
