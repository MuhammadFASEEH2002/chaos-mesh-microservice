apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-inventory-experiment
  namespace: default
spec:
  action: pod-kill
  mode: all
  selector:
    labelSelectors:
      app: inventory-service
  duration: "60s"
  gracePeriod: 0
