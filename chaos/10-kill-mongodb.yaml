apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-mongodb-experiment
  namespace: default
spec:
  action: pod-kill
  mode: all
  selector:
    labelSelectors:
      app: mongodb
  duration: "60s"
  gracePeriod: 0
