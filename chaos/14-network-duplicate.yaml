apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-duplicate-experiment
  namespace: default
spec:
  action: duplicate
  mode: all
  selector:
    labelSelectors:
      app: user-service
  duplicate:
    duplicate: "30"
    correlation: "50"
  duration: "60s"
