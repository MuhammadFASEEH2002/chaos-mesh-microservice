apiVersion: chaos-mesh.org/v1alpha1
kind: HTTPChaos
metadata:
  name: http-abort-experiment
  namespace: default
spec:
  mode: fixed
  value: "2"
  selector:
    labelSelectors:
      app: api-gateway
  target: Request
  port: 3000
  path: "/api/orders*"
  abort: true
  duration: "300s"
